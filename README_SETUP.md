# Инструкция по установке и настройке проекта "Политех-ЕАО"

## 📋 Обзор проекта

Это веб-приложение для образовательной организации "Политех-ЕАО" с административной панелью для управления контентом сайта. Проект включает фронтенд (React + TypeScript + Vite) и бэкенд (Node.js + Express + Prisma + PostgreSQL).

## 🛠️ Системные требования

- **Node.js** версии 18.0.0 или выше
- **PostgreSQL** версии 13.0 или выше
- **npm** или **yarn** для управления пакетами
- **Git** для клонирования репозитория

## 📁 Структура проекта

```
politeh-eao/
├── backend/          # Серверная часть (Node.js + Express)
├── frontend/         # Клиентская часть (React + TypeScript)
├── uploads/          # Загруженные файлы
└── README_SETUP.md   # Эта инструкция
```

## 🚀 Шаг 1: Установка зависимостей

### 1.1 Клонирование репозитория

```bash
git clone <repository-url>
cd politeh-eao
```

### 1.2 Установка зависимостей бэкенда

```bash
cd backend
npm install
```

### 1.3 Установка зависимостей фронтенда

```bash
cd ../frontend
npm install
```

## 🗄️ Шаг 2: Настройка базы данных

### 2.1 Установка PostgreSQL

Установите PostgreSQL на вашу систему:

**Windows:**
- Скачайте с [postgresql.org](https://www.postgresql.org/download/windows/)
- Следуйте инструкциям установщика
- Запомните пароль пользователя `postgres`

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

**macOS:**
```bash
brew install postgresql
brew services start postgresql
```

### 2.2 Создание базы данных

```bash
# Войдите в PostgreSQL как суперпользователь
psql -U postgres

# Создайте базу данных для проекта
CREATE DATABASE politeh;

# Создайте пользователя для проекта (опционально)
CREATE USER politeh_user WITH PASSWORD 'your_password_here';

# Дайте права пользователю на базу данных
GRANT ALL PRIVILEGES ON DATABASE politeh TO politeh_user;

# Выйдите из PostgreSQL
\q
```

### 2.3 Настройка переменных окружения

Создайте файл `.env` в папке `backend/`:

```env
# База данных
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/politeh"

# JWT секрет для аутентификации
JWT_SECRET="your_super_secret_jwt_key_here_make_it_long_and_random"

# Порт сервера
PORT=5000

# URL фронтенда для CORS
FRONTEND_URL="http://localhost:3000"
```

**Важно:** Замените `your_password` на реальный пароль от PostgreSQL, а `your_super_secret_jwt_key_here_make_it_long_and_random` на случайную строку для JWT токенов.

## 🔧 Шаг 3: Инициализация базы данных

### 3.1 Генерация Prisma клиента

```bash
cd backend
npx prisma generate
```

### 3.2 Создание и применение миграций

```bash
# Создание миграций на основе схемы
npx prisma migrate dev --name init

# Или применение существующей схемы
npx prisma db push
```

### 3.3 Создание администратора

```bash
# Запустите скрипт создания администратора
node create-admin.js
```

Скрипт попросит ввести:
- Имя пользователя администратора
- Пароль администратора

## 🌐 Шаг 4: Запуск проекта

### 4.1 Запуск бэкенда

```bash
cd backend
npm run dev
```

Сервер запустится на `http://localhost:5000`

### 4.2 Запуск фронтенда

В новом терминале:

```bash
cd frontend
npm run dev
```

Фронтенд запустится на `http://localhost:3000`

## 🔐 Шаг 5: Доступ к административной панели

1. Откройте браузер и перейдите на `http://localhost:3000`
2. Перейдите в раздел "Админ-панель" или напрямую на `http://localhost:3000/admin`
3. Введите логин и пароль администратора, созданные в шаге 3.3

## 📊 Шаг 6: Структура разделов админ-панели

### Основные разделы:

- **Главная страница** - управление слайдером и основным контентом
- **Новости** - добавление и редактирование новостей
- **Расписание** - управление учебным расписанием
- **Студенты** - управление информацией о студентах
- **Документы** - управление документами и файлами

### Раздел "Сведения об организации":

- **Общие сведения** - основная информация об организации
- **Структура** - организационная структура
- **Документы** - организационные документы
- **Образование** - образовательные программы
- **Руководство** - информация о руководстве
- **Педагогический состав** - список преподавателей
- **Материально-техническое обеспечение** - оборудование и помещения
- **Стипендии и меры поддержки** - информация о стипендиях
- **Платные образовательные услуги** - платные программы
- **Финансово-хозяйственная деятельность** - бюджет и финансы
- **Международное сотрудничество** - международные связи
- **Организация питания** - объекты питания

## 📁 Шаг 7: Работа с файлами

### Структура папок для загрузок:

```
backend/uploads/
├── images/          # Изображения
├── documents/       # Документы
├── objects/         # Файлы материально-технического обеспечения
├── grants/          # Документы стипендий
├── budget/          # Финансовые документы
└── paid-edu/        # Документы платных услуг
```

### Поддерживаемые форматы файлов:

- **Изображения:** JPG, PNG, GIF, WebP
- **Документы:** PDF, DOC, DOCX, XLS, XLSX
- **Максимальный размер:** 10MB на файл

## 🔧 Шаг 8: Полезные команды

### Бэкенд:

```bash
# Запуск в режиме разработки
npm run dev

# Запуск в продакшене
npm start

# Перезапуск сервера
npm restart

# Генерация Prisma клиента
npx prisma generate

# Создание миграции
npx prisma migrate dev --name migration_name

# Просмотр состояния БД
npx prisma studio
```

### Фронтенд:

```bash
# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр сборки
npm run preview

# Линтинг кода
npm run lint
```

## 🐛 Шаг 9: Устранение неполадок

### Проблема: "Cannot read properties of undefined (reading 'findMany')"

**Решение:**
```bash
cd backend
npx prisma generate
npm restart
```

### Проблема: "Connection refused" при подключении к БД

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте правильность `DATABASE_URL` в `.env`
3. Убедитесь, что база данных существует

### Проблема: "Port already in use"

**Решение:**
```bash
# Найти процесс, использующий порт
netstat -ano | findstr :5000
# Или на Linux/Mac:
lsof -i :5000

# Убить процесс
taskkill /PID <PID> /F
```

### Проблема: Ошибки загрузки файлов

**Решение:**
1. Проверьте права на папку `backend/uploads/`
2. Убедитесь, что папки существуют
3. Проверьте ограничения на размер файлов

## 📞 Шаг 10: Поддержка

При возникновении проблем:

1. Проверьте логи сервера в терминале
2. Убедитесь, что все зависимости установлены
3. Проверьте конфигурацию в файлах `.env`
4. Перезапустите серверы

## 🔒 Шаг 11: Безопасность

### Важные рекомендации:

1. **Не коммитьте `.env` файл** в Git
2. **Используйте сильные пароли** для администратора
3. **Регулярно обновляйте** зависимости
4. **Настройте бэкапы** базы данных
5. **Используйте HTTPS** в продакшене

## 📝 Шаг 12: Разработка

### Добавление новых разделов:

1. Создайте модель в `backend/prisma/schema.prisma`
2. Создайте маршруты в `backend/src/routes/`
3. Создайте компоненты в `frontend/src/pages/`
4. Добавьте в навигацию и админ-панель

### Стиль кода:

- Используйте TypeScript для типизации
- Следуйте конвенциям React и Express
- Используйте ESLint для проверки кода
- Документируйте сложную логику

---

## ✅ Проверка установки

После выполнения всех шагов проверьте:

- [ ] Бэкенд запускается без ошибок
- [ ] Фронтенд компилируется без ошибок
- [ ] Можно войти в админ-панель
- [ ] Можно загружать файлы
- [ ] Все разделы админ-панели работают

**Поздравляем!** Проект успешно установлен и готов к работе.